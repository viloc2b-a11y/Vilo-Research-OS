# pharmacy-manuals (reserved)

Reserved validation-corpus category. No pharmacy manual source documents exist in the
repository today. Add sanitized pharmacy manuals here (with a `*.mapping.json` per
document) when source material becomes available.
