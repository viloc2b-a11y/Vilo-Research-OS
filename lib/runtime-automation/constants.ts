export const RUNTIME_AUTOMATION_VERSION = 1

/** Max proposed actions per visit cycle when overload detected. */
export const OVERLOAD_MAX_ACTIONS = 5

/** Default max when not overloaded. */
export const DEFAULT_MAX_ACTIONS = 12
