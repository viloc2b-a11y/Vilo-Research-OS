# Supabase project: "Vilo Research OS" (staging)
# Dashboard: https://supabase.com/dashboard → Vilo Research OS → Settings → API

# Deploy (npm run deploy) — pushes current branch to origin; Cloudflare Pages builds from git
# DEPLOY_GIT_REMOTE=origin
# DEPLOY_GIT_BRANCH=staging/pilot-runtime
# DEPLOY_SKIP_BUILD=1
# DEPLOY_SKIP_PUSH=1

# Browser-safe (Cloudflare Pages / local)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...anon...

# Server-only — never NEXT_PUBLIC_
SUPABASE_SERVICE_ROLE_KEY=eyJ...service...role...

# One-time internal bootstrap (npm run db:bootstrap-org-owner) — service role only
# BOOTSTRAP_ORG_OWNER_EMAIL=jmendez@viloresearchgroup.com
# BOOTSTRAP_ORG_ID=
# BOOTSTRAP_ORG_NAME=

# Operational Calendar QA coordinator seed (npm run db:seed-operational-calendar-coordinator)
# CALENDAR_QA_STUDY_ID=6bae715a-8536-4000-8d24-22b6a3dbb8c9
# CALENDAR_QA_ORG_ID=
# CALENDAR_QA_COORDINATOR_EMAIL=calendar.qa.coordinator@vilo-os.staging

# Postgres for npm run db:migrate (postgres.js)
# Prefer DIRECT URI for DDL (avoids pooler "Tenant or user not found" issues):
# DATABASE_URL_DIRECT=postgresql://postgres:[PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres
#
# Transaction pooler (6543): username must be postgres.[PROJECT_REF]; URL-encode password.
# Migrate script sets prepare:false for *.pooler.supabase.com — required for postgres.js + PgBouncer.

DATABASE_URL=postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-[region].pooler.supabase.com:6543/postgres

# Site calendar timezone for Operational Calendar date rendering (IANA name).
# Defaults to America/Chicago when unset. Set both so server and client agree.
NEXT_PUBLIC_SITE_TIMEZONE=America/Chicago
SITE_TIMEZONE=America/Chicago

# VPI feature flags (lib/performance/read-layer)
# When true, /performance uses vpi_load_dashboard RPC with automatic signal fallback.
VPI_USE_RPC=true
# Scoring lite (Phase 7C). Set false only for rollback; default is on when unset.
VPI_SCORING_ENABLED=true

# Phase 11/14 live runtime pilot (staging — phase2-validation-study / PHASE9A-PILOT-001)
# Run: npm run runtime:pilot-fixture && npm run runtime:e2e:live -- --fail-on-fail
PHASE11_STUDY_ID=6bae715a-8536-4000-8d24-22b6a3dbb8c9
PHASE11_ORG_ID=f7cf7d2b-49cd-4bcd-9e15-2675966c3e1e
PHASE11_SUBJECT_ID=4384b789-4e16-4512-b3f3-50642b3b9735
PHASE11_VISIT_ID=6690da63-4bf1-4681-815a-3e39b7b014bc
PHASE11_COORDINATOR_ACTOR_ID=d7e43ee5-5c08-489b-b293-8ef288e7fdb7

# OpenRouter server-side API access.
# Server-only. Do not expose this value to browser/client bundles.
OPENROUTER_API_KEY=your_server_side_openrouter_key
